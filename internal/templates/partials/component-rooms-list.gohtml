{{define "partials/component-rooms-list.html"}}
{{ if len .rooms }}
<div class="space-y-2">
    {{ range .rooms }}
    <a href="/rooms/{{.ID}}" hx-get="/api/rooms/{{.ID}}/chat-content" hx-target="#content" hx-swap="innerHTML" hx-push-url="/rooms/{{.ID}}" class="block p-3 bg-gray-50 dark:bg-gray-700 hover:bg-gray-100 dark:hover:bg-gray-600 rounded-md cursor-pointer">
        <p class="font-medium text-gray-800 dark:text-gray-100">{{ .Name }}</p>
        <p class="text-sm text-gray-500 dark:text-gray-400">
            {{ if .CreatedAt.IsZero }}
            Created recently
            {{ else }}
            Created {{ .CreatedAt.Format "Jan 2, 2006" }}
            {{ end }}
        </p>
    </a>
    {{ end }}
</div>
{{ else }}
<p class="text-gray-500 dark:text-gray-400">No rooms available. Create one to get started.</p>
{{ end }}
{{end}}